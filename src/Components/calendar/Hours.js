import React, { useState } from "react";
import { format, addHours } from "date-fns";

const Hours = props => {
    while(props.time.getHours() <= 24){
        
    }

  return <div></div>;
};

export default Hours;
